# Web server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
itsdangerous==2.2.0
# >=2.10.6 required by tiktok-uploader; pinning 2.10.4 broke resolution.
pydantic>=2.10.6

# Pipeline
# Kept unpinned on purpose: YouTube breaks older yt-dlp releases constantly,
# so always install the latest. Run `pip install -U yt-dlp` if downloads fail.
yt-dlp
faster-whisper==1.1.0
google-genai==0.8.0
opencv-python-headless==4.11.0.86
numpy==2.2.1
edge-tts==7.0.0
# Kokoro-82M TTS (realistic, offline, CPU) — default voice engine. Unpinned so
# the onnxruntime-compatible build resolves for your Python; pulls misaki+espeak.
kokoro-onnx

# YouTube upload / OAuth (Copy auto-publish factory)
google-api-python-client==2.155.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# Headless-browser automation (remote login sessions + fallback uploader).
# Left unpinned above 1.58 because tiktok-uploader requires >=1.58.0; pinning
# 1.49 made pip fail with ResolutionImpossible.
playwright>=1.58.0
# Preferred automatic TikTok uploader: duplicates the logged-in browser cookies
# we capture through the dashboard. https://github.com/wkaisertexas/tiktok-uploader
tiktok-uploader==1.2.0

# Optional public tunnel (used by run.sh --tunnel)
pyngrok==7.2.3
